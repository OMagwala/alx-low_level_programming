#include <stdio.h>
/**
 * main - prints a string using printf()
 * Return: returns 0 when the programme is done
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
